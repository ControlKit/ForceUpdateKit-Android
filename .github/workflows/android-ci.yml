name: Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper/
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle

      # ✅ اجرای درست تست ها برای ماژول forceUpdatekit
      - name: Run unit tests
        run: ./gradlew clean :forceUpdatekit:testDebugUnitTest --stacktrace

      # ✅ تولید گزارش کاورج برای همون ماژول
      - name: Generate JaCoCo report
        run: ./gradlew :forceUpdatekit:jacocoTestReport

      # ✅ پرینت درصد کاورج از تسک سفارشی
      - name: Print coverage percentage
        run: ./gradlew :forceUpdatekit:printCoverage

      # ✅ آپلود گزارش HTML
      - name: Upload JaCoCo HTML report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: forceUpdatekit/build/reports/jacoco/jacocoTestReport/html
          if-no-files-found: error
